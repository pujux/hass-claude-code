#include <tunables/global>

profile claude_code flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  capability dac_read_search,
  capability ptrace,

  network,

  /dev/ptmx rw,
  /dev/pts/* rw,

  /bin/bash ix,
  /usr/bin/bash ix,
  /usr/bin/python3* ix,
  /root/.local/bin/claude ix,
  /root/.local/bin/hass-mcp ix,
  /root/.bun/bin/bun ix,

  # uv — combine read+exec in one rule to avoid duplicate path conflict
  /root/.local/share/uv/** rix,

  /root/.bun/** mr,
  /app/** mr,

  /homeassistant/.claudecode/** rwk,

  /tmp/claude-paste/** rwk,
  /tmp/** rw,

  /data/** rwk,

  /homeassistant/** rw,
  /share/** rw,
  /media/** rw,
  /ssl/** r,
  /addon_config/** rw,

  /etc/** r,
  /proc/** r,
  /sys/fs/cgroup/** r,

  deny /data/supervisor/** rwx,
}
